name: "Fast Forward Reports"

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  reports:
    uses: php-fast-forward/dev-tools/.github/workflows/reports.yml@main
    secrets: inherit
